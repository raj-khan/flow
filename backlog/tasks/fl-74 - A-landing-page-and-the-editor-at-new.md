---
id: FL-74
title: 'A landing page, and the editor at /new'
status: Done
assignee:
  - '@pi'
created_date: '2026-09-25 17:39'
updated_date: '2026-09-25 18:03'
labels:
  - seo
  - frontend
milestone: m-1
dependencies:
  - FL-73
priority: high
type: feature
ordinal: 4000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->

Today `/` redirects straight into the client-rendered editor, so there is nothing for search engines or AI fetchers to read. Give `/` a prerendered landing page and move the editor to `/new` (the Excalidraw one-click-to-canvas pattern).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria

<!-- AC:BEGIN -->

- [x] #1 / is prerendered static HTML: the loop in one line, a live canvas to try, a short clip of sketch to Copy for AI to Claude building it, and the MCP setup line
- [x] #2 The editor lives at /new; /flow and existing share-hash links still open
- [x] #3 vercel.json rewrites only app routes to the SPA; content pages are served as static HTML
- [x] #4 e2e covers the landing page and the redirect of old links

<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->

1. Router: the editor's canonical path becomes /new with /flow as its alias (old links and #flow= share links unchanged), / redirects to it as the SPA fallback. 2. landing.template.html + scripts/make-landing.mjs (npm run landing) generate public/landing.html: the loop one-liner, the static diagram SVG, an iframe of the real editor seeded with the architecture example (live canvas), the brief with a Copy button, and the MCP setup lines; a Vite middleware serves it at / in dev and preview. 3. scripts/make-demo-clip.mjs (npm run clip) records the real app — sketch, Copy for AI, Claude building — as public/demo.webm for the landing. 4. vercel.json rewrites only /new and /flow (+children) to the SPA; / to the landing; nginx likewise. 5. sitemap gains /new. 6. e2e/landing.spec.js covers the landing content and that /flow, /new and a share-hash link all open the editor.

<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->

Editor canonical at /new with /flow as alias (children incl. node/:id resolve under both), / redirects to it as SPA fallback. public/landing.html generated by npm run landing from landing.template.html: loop one-liner, static renderSvg fallback, live iframe of the real editor seeded with the architecture example share hash, Copy-for-AI brief block with copy button, MCP stdio+http setup lines, own meta head. npm run clip records public/demo.webm (real app: drag, inline rename, Copy for AI, chat overlay of the agent building). vite middleware serves landing at / in dev+preview; vercel.json rewrites only /new and /flow (+ children) to index.html and / to landing.html; nginx location = / serves landing.html. Sitemap lists / and /new. Verified: e2e/landing.spec.js 3 tests (landing content + iframe canvas + old /flow + share-hash link + both pages on one server) and the full 78-test e2e suite pass; unit 235 pass; typecheck, lint clean.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->

The landing is a generated static page at / (loop in one line, live editor iframe seeded with an example, a recorded clip of sketch→Copy for AI→agent building, and the MCP setup lines), the editor moved to /new with /flow kept as an alias so old and share-hash links open unchanged, vercel.json/nginx route only app paths to the SPA, and e2e covers the landing plus both link forms. Verified by the full Playwright suite (81 e2e including 3 new) and the unit suite.
<!-- SECTION:FINAL_SUMMARY:END -->
